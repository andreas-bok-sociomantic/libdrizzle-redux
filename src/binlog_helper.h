uint8_t drizzle_binlog_rows_event_version ( drizzle_binlog_event_types_t column_type );

void dump_array_to_hex(const unsigned char *ptr, uint32_t len);

uint32_t ptr_dist(unsigned char* ptr1, unsigned char* ptr2);
